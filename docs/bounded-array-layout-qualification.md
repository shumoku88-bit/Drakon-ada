# Temporary bounded-array layout qualification

The Phase A1 witness is generated by `tools/bounded_array_fixture.py`; neither
canonical countdown nor pinned upstream is changed. Four slots remain a
qualification witness, not a production limit. Human visual approval is pending.

## Fit and topology

Keep the pinned headless action fit: initialization half-extents `(50, 30)` and
fold half-extents `(110, 30)`. Raw text metrics alone are not stored dimensions.
The original fold corridor had width 220, exactly the fitted action's full width.
`graph2.tcl:icons.cut.segments` includes rectangle boundaries when cutting lines;
contact with both adjacent verticals caused `All icons must be connected together`
before semantic generation. Shrinking the action is not a fix.

Keep the main trunk fixed to avoid translating header, initialization and exit.
Derive the smallest nonnegative 10-unit-grid shifts satisfying a 10-unit gap on
each side: move fold action and its vertical by 10, and the outer arrow lane by
20. Update the if's right junction and both arrow arms consistently. The pinned
`icon.if.tcl` right endpoint is `x+w+a`; `icon.arrow.tcl` upper return and lower
entry are `x-w` and `x-a`. The fixture checks this specific topology before making
any translation; this is not a new general layout engine.

Final layout:

- Main trunk: x=180 (unchanged).
- Fold action: center (300, 300), w=110, h=30; bounds x=190..410, y=270..330.
- Fold vertical: x=300, y=240..360.
- If right junction: x=300, y=240; a=60 (was 50).
- Loopback: x=420, y=180..360; w=240, a=120.
- Upper return remains (180, 180); lower entry moves to (300, 360).
- Corridor width: 240; left and right clearance: 10 each.

With the trunk fixed, the fold must move at least 10 to clear it; the outer lane
must then move at least 20 to clear the fold. These bounds are attained without
changing any y-coordinate or unrelated icon. Tests check fitted size, grid,
clearance, endpoint equations and actual upstream graph/Ada generation together.

## Mac technical qualification

Existing GNAT 16.1.0, GPRbuild reporting 26.0.0 (installed in the specified
26.0.1 toolchain directory), GNATprove FSF 16.1.0; no install/update.

- Focused unittest: 7 pass (original 6 plus geometry/connectivity regression).
- Focused generation: pass.
- GNATprove: 10 checks, Flow 3 / Provers 7; Assume 0, Justified 0, Unproved 0.
- Exhaustive runtime: all 194,481 inputs pass.
- Negative controls: off-by-one index, too-narrow Quantity and weak invariant
  all compile and fail proof with the expected diagnostics.
- Full unittest: 27 pass.
- Full `verify.py`: pass, including existing Movement (3 checks; 1,000,000
  runtime inputs) and control-flow (11 checks; 32 inputs; negative controls).

`verify.py` deletes `build/`. Regenerate the temporary review file afterwards:

```sh
python3 tools/bounded_array_fixture.py
tclsh integration/drakon-editor/generate.tcl \
  build/bounded-array-support/array_fold.drn \
  build/bounded-array-support/generated
```

Headless metrics do not establish actual GUI font fit or readability. Open
`build/bounded-array-support/array_fold.drn` in DRAKON Editor and wait for the
user's visual review. PR #2 stays Draft; no canonical promotion, merge or Phase A2.
